#include <algorithm>
#include <cstdlib>
#include <fstream>
#include <iostream>
#include <numeric>
#include <set>
#include <sstream>
#include <string>
#include <utility>
#include <vector>

using ull = unsigned long long;
using sll = long long;

int main(int argc, char *argv[]) {
  std::ifstream fin;
  fin.open(argv[1]);

  std::string inputval;

  while (std::getline(fin, inputval)) {
    std::string input;
  }

  // std::cout << part1 << '\n';
  // std::cout << part2 << '\n';

  return EXIT_SUCCESS;
}
